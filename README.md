# Eyelash Corne — ZMK Config

Personal ZMK configuration for the [Eyelash Peripherals Corne](https://github.com/a741725193/zmk-new_corne):
Workman alphas, bilateral home-row mods, a dwm window-manager cluster, and
[nice-view-gem](https://github.com/M165437/nice-view-gem) on both screens.

> **This keyboard is not [foostan's Corne](https://github.com/foostan/crkbd).**
> It will not build or run with standard `corne` firmware.

## Keymap

![Keymap](keymap-drawer/eyelash_corne.svg)

The diagram is regenerated by the **Draw Keymap** workflow on every push, so it
always matches `config/eyelash_corne.keymap` — never edit the SVG by hand.

### Layers

| # | Name | Reached by |
| --- | --- | --- |
| 0 | `WORKMAN` | base |
| 1 | `NAV` | hold left thumb `TAB` |
| 2 | `MOUSE` | hold left thumb `SPACE` |
| 3 | `TAGS` | `G` + `Y` combo (dwm tag switching) |
| 4 | `MEDIA` | hold left outer thumb `ESC` |
| 5 | `NUM` | hold right thumb `BACKSPACE` |
| 6 | `SYM` | hold right thumb `ENTER` |
| 7 | `FUN` | hold right thumb `DELETE` |
| 8 | `GAME` | `&tog 8` — toggled, not held |

### Combos

| Keys | Does |
| --- | --- |
| `V` + `K` | caps word (base layer only) |
| `A` + `I` | Vicinae toggle (`Gui+;`) |
| `G` + `Y` | hold `TAGS` layer |
| `Q` + `S` + `Z`, held 2s | soft off — wake with a **single** reset press |

Combo pairs are chosen to avoid common English bigrams, which matters at
speed: caps word sat on `T` + `N` until `"nt"` (want, point, into, count)
made it misfire. `"vk"` does not occur in English. All of them also carry
`require-prior-idle-ms = 200`, so no combo can fire during a typing burst —
only after a deliberate pause.

### Bluetooth

On the `MEDIA` layer, the bottom row is `BT_SEL 0`–`4`, then `BT_CLR` (drops the
current profile only). `OUT_TOG` and `OUT_BLE` are on the same layer for
USB/BLE endpoint control.

Two things worth knowing, both learned the hard way:

- **`&bt BT_SEL` is 0-indexed.** A keymap that binds profiles `1`–`4` has no key
  for profile `0` — and `BT_CLR_ALL` resets the active profile to exactly that
  one, so clearing your bonds strands you on the profile you cannot select.
- **`BT_CLR_ALL` is not bound at all.** It used to sit under a letter on the
  `MEDIA` layer, one stray thumb away from wiping every host bond, which is
  exactly what happened once. `BT_CLR` drops a single profile if one goes bad;
  to wipe everything, flash the `settings_reset` firmware deliberately.

Bonds live in the settings partition and **survive a normal `.uf2` flash**, so
you can iterate on the keymap without re-pairing. Only the `settings_reset`
build clears them.

## Fork changes

This fork diverges from upstream in ways that affect **how the firmware is
built**, not just which keys do what. Read this before building.

### nice!view screens run nice-view-gem

Both halves use [nice-view-gem](https://github.com/M165437/nice-view-gem)
in place of ZMK's stock `nice_view` status screen.

| File | Change |
| --- | --- |
| `config/west.yml` | adds the `M165437` remote, pins `nice-view-gem` to `v0.3.0` |
| `build.yaml` | shield is `nice_view_gem` on both halves |
| `config/eyelash_corne.conf` | sets `CONFIG_ZMK_DISPLAY=y` |

Three things that are easy to get wrong:

1. **Keep the revisions in step.** `nice-view-gem` is pinned to `v0.3.0` to
   match the ZMK v0.3 base in `west.yml`. Upstream is explicit that ZMK and
   the module must be on matching revisions. The module's `main` branch
   targets Zephyr 4.1+ and will not work here.
2. **`nice_view_adapter` is not needed**, despite what nice-view-gem's
   README shows in its Kyria example. That adapter exists to synthesize a
   `nice_view_spi` node for shields wired to the nice!oled header. This
   shield already defines one (`eyelash_corne.dtsi`), and gem's overlay is
   identical to stock `nice_view`'s, so it binds directly.
3. **`CONFIG_ZMK_DISPLAY=y` has to be set in the config.** No shield turns
   it on — not this one, and not stock `nice_view` either. Without it both
   screens stay dark whichever display shield you pick.

Gem's tunables (animation, WPM gauge range, inversion) sit commented at
their defaults in `config/eyelash_corne.conf`.

To return to stock: set the shield back to `nice_view` in `build.yaml` and
drop the `nice-view-gem` project from `west.yml`.

### Keymap safety

- Thumb keys use a guarded layer-tap (`ltq`) rather than the stock `&lt`, with
  `quick-tap-ms` and `require-prior-idle-ms` set, so a thumb held slightly long
  mid-word can no longer swap in a layer under your fingers.
- The left mod row on `NAV` / `MOUSE` / `MEDIA` uses sticky mods (`&sk`) instead
  of held ones, so a stray press cannot latch a modifier across a run of
  keystrokes.

Both exist because the stock arrangement produced accidental `Ctrl+R` while
typing — a held thumb engaged a layer, which put a raw `LCTRL` under the `H`
key, and the next letter became a modifier chord.

## Building

Push to `main`. The **Build ZMK firmware** workflow produces a `firmware`
artifact containing:

| File | Flash to |
| --- | --- |
| `eyelash_corne_studio_left.uf2` | left half (central, ZMK Studio enabled) |
| `eyelash_corne_right-nice_nano_v2-zmk.uf2` | right half (peripheral) |
| `settings_reset-nice_nano_v2-zmk.uf2` | **only** to erase bonds and settings |

To flash: plug USB into the half being flashed, **double-tap** reset, then copy
the `.uf2` onto the `NICENANO` drive that appears.

```bash
cp eyelash_corne_studio_left.uf2 /run/media/$USER/NICENANO/
sync
```

The board reboots the instant the write completes, which pulls the USB device
out from under the filesystem — so `cp` often ends in `Input/output error` and
`sync` may hang briefly. **That is normal and the flash succeeded.** The drive
disappearing is the real confirmation.

Do the right half first: while the left is in bootloader mode it is not acting
as a keyboard.

## Credits

- [a741725193/zmk-new_corne](https://github.com/a741725193/zmk-new_corne) — the
  board definition and shield this config builds against. For a 3D model of the
  case, that repo lists `380465425@qq.com`.
- [M165437/nice-view-gem](https://github.com/M165437/nice-view-gem) — display
  status screen.
- [caksoylar/keymap-drawer](https://github.com/caksoylar/keymap-drawer) —
  generates the keymap diagram above.
